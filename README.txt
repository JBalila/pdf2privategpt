To run Flask backend (via Ubuntu command line):
	cd into 'backend' folder
	type 'source venv/bin/activate'
	type 'flask run'

To run frontend:
cd into 'frontend' folder and type 'npm start' into Windows command-line